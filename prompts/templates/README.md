# Prompts Templates

This directory contains templates for prompts that can be used to generate prompts for various tasks.

## Usage

- Copy the relevant template into the `tmp/` directory and edit it as needed.
- Replace any variables in the template with the actual values. e.g. `THE_FILE` -> `update_tasks`.
- Paste the update template into the Agent Chat to run the prompt.