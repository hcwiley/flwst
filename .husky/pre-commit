#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Enforce linting and formatting before code leaves the workstation.
CI=${CI:-true} pnpm lint || exit 1
CI=${CI:-true} pnpm format:check || exit 1
