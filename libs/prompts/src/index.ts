/**
 * @flwst/prompts — default prompt templates for the MVP pipeline.
 * Source of truth for dailyNote and taskDraft; config stores overrides only.
 */

export type { PromptDefinition } from './definitions';
export { dailyNote, taskDraft, defaultPrompts } from './definitions';
